import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 20) {
            Text("XcodeBuildDemo")
                .font(.largeTitle)
            Text("Built with xcodebuild")
                .font(.subheadline)
                .foregroundColor(.secondary)
        }
    }
}
